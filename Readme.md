#Lec6

whenever state variable changes, it rerender component
react compares older virtual dom object and newer virtual dom object and it updates only that dom which has changed
react is faster because of reconciliation algorithm, it only updates dom element which has changed
whenever state updates, react triggers reconcilation cycle and rerenders component,

Search functionality